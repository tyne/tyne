<div class="span8 offset3 well">
  <div class="importers">
    <h1>Import Assistant</h1>
    <p class="lead">
    Please select one of the following import providers to import projects from a different
    system into Tyne.
    </p>

    <ul>
      <% @importers.each do |key, value| %>
        <li>
          <h2><%= t("importers.#{key}.display_name") %></h2>
          <p><%= t("importers.#{key}.description") %></p>
          <%= link_to "Import from #{I18n.t("importers.#{key}")}.display_name", provider_import_path(:provider => key) %>
        </li>
      <% end %>
      <ul>
    <br />
  </div>
</div>
