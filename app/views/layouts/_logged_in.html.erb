<ul class="nav">
  <% if @project && @project.persisted? && !admin_area?%>
    <li><%= link_to "Backlog", main_app.issues_path(:user => @project.user.username, :key => @project.key) %></li>
    <li><%= link_to "Admin", main_app.admin_project_path(:user => @project.user.username, :key => @project.key) %></li>
  <% end %>
</ul>

<ul class="nav pull-right">
  <li>
    <%= link_to "New Project", tyne_core.new_project_path %>
  </li>
  <li class="dropdown">
    <a class="dropdown-toggle" data-toggle="dropdown" href="#" id="user_dropdown">
      <%= avatar(current_user) %>
      <b class="caret"></b>
    </a>
    <ul class="dropdown-menu">
      <li><%= link_to "Logout", main_app.logout_path, :method => :delete %></li>
    </ul>
  </li>
</ul>
